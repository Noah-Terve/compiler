template @l T, hello @r
T test2(T x, hello y) {
    T w = x;
    hello z = y;
    return w;
}

template @l T, hello @r
T test(T x, hello y) {
    T w = x;
    hello z = y;

    test2 @l string, T @r("hello", 'e');
    return w;
}

test @l int, int @r (5, 4);