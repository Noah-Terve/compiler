struct dog {
    int legs;
};

int test (dog s) {
    dog d;
    s.legs;
    s.legs = 4;
    return s.legs;
    /* return s.legs; */
}

int test2 (int a) {
    dog d;
    int a = test(d);
    return a;
}

print @l int @r (test2(5));
